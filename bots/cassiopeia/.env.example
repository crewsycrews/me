# Separate bot for leads; do not reuse the channel publishing bot.
BOT_TOKEN=
BOT_USERNAME=casiq_cassiopeia_bot
# Leave blank initially, send /whoami to the bot, then set your positive ID.
OWNER_CHAT_ID=
HTTP_PORT=3088

# Optional: enable only after installing the Telegram-only Xray proxy.
# NODE_USE_ENV_PROXY=1
# HTTPS_PROXY=http://telegram-proxy:1080
# NO_PROXY=localhost,127.0.0.1,::1
