{
  "name": "cassiopeia-leads",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.16.0 <25" },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/main.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
